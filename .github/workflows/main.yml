name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    name: Build APK with Buildozer
    runs-on: ubuntu-latest

    steps:
      - name: 📥 检出代码
        uses: actions/checkout@v4

      - name: 🐍 设置 Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: 🔧 安装 Buildozer 和 Cython
        run: |
          python -m pip install --upgrade pip
          pip install buildozer
          pip install "cython==0.29.36"

      - name: 📦 安装系统依赖
        run: |
          sudo dpkg --add-architecture i386
          sudo apt-get update
          sudo apt-get install -y \
            git zip unzip openjdk-17-jdk \
            autoconf automake libtool pkg-config \
            libffi-dev libssl-dev libxml2-dev libxslt1-dev \
            zlib1g-dev libncurses5-dev libbz2-dev libreadline-dev \
            libsqlite3-dev wget curl llvm libncursesw5-dev \
            xz-utils tk-dev libffi-dev liblzma-dev

          sudo update-alternatives --set java /usr/lib/jvm/java-17-openjdk-amd64/bin/java
          sudo update-alternatives --set javac /usr/lib/jvm/java-17-openjdk-amd64/bin/javac

      - name: ⚙️ 预安装 Android SDK 并接受许可证（关键！）
        env:
          ANDROID_HOME: /home/runner/.buildozer/android/platform/android-sdk
          PATH: $PATH:/home/runner/.buildozer/android/platform/android-sdk/cmdline-tools/latest/bin
        run: |
          # 创建 cmdline-tools 目录结构（Buildozer 期望的路径）
          mkdir -p /home/runner/.buildozer/android/platform/android-sdk/cmdline-tools/latest

          # 下载 SDK Command-line Tools
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -O cmdline-tools.zip
          unzip -q cmdline-tools.zip -d /tmp/cmdline
          mv /tmp/cmdline/cmdline-tools/* /home/runner/.buildozer/android/platform/android-sdk/cmdline-tools/latest/

          # 接受所有许可证（非交互式）
          yes | sdkmanager --licenses > /dev/null 2>&1 || true

          # 可选：预安装常用组件（加速后续 buildozer 步骤）
          sdkmanager "platforms;android-31" "build-tools;31.0.0" "ndk;25.1.8937393" "platform-tools"

      - name: 🛠️ 构建 APK
        run: |
          # 强制 Buildozer 使用我们已配置好的 SDK
          buildozer -v android debug

      - name: 📤 上传 APK
        uses: actions/upload-artifact@v4
        with:
          name: inspection-tool-apk
          path: bin/*.apk
          if-no-files-found: error
